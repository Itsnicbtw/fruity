# Fruity admin login.
# Copy to .env and replace all values before production.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-long-random-password
SESSION_SECRET=replace-with-at-least-32-random-characters

PORT=3000
NODE_ENV=development
